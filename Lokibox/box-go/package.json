{
  "name": "box-server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts help",
    "proxy": "bun run index.ts proxy",
    "connect": "bun run index.ts connect",
    "listen": "bun run index.ts listen",
    "raw": "bun run index.ts raw",
    "test:protocol": "bun run index.ts test",
    "test:vec3": "bun run index.ts vec3",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "mudb": "1.0.1"
  }
}
