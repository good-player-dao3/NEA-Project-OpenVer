# 为 NEA Project OpenVer 做出贡献

感谢您帮助保护和实现 DAO3 运行时证据基础。

## 在修改代码之前

1. 阅读[仓库布局](docs/repository-layout.md)和[开放版本政策](docs/open-version.md)。
2. 找到拥有该行为的现有运行时层。不要引入并行的 Player 或脚本运行时。
3. 定位直接的本地证据：文档、声明、历史捆绑包、已记录的传输、保留的运行时代码或真实脚本用法。
4. 当证据无法确立行为时，记录兼容性缺口，而不是进行猜测。

## 更改边界

- 保持客户端脚本运行时、服务器脚本运行时、MuDB 传输和权威运行时更改相互分离且明确。
- 优先采用范围狭窄、可审查且带有具体验收条件的更改。
- 当源行为或证据映射发生变化时，重新生成 `runtime-compat/` 下受影响的 ABI/报告工件。
- 在受影响的运行时包旁边添加或更新一致性测试覆盖。
- 在 Windows 工作区中工作时，使用 `tools/apply_patch.ps1` 进行仓库编辑。

## 切勿提交

- 浏览器配置文件、Cookie 存储、OAuth 数据、密码、会话令牌或含有令牌的 URL。
- `dump/private/`、`works/private/`、`Lokibox/`、`.workspace/`、`NEA-Project.7z` 或私有地图/脚本源代码。
- 大型重建输出，如果它们可以确定性重新生成，除非现有政策明确要求跟踪它们。

## 证据贡献

在更改的文档或生成器元数据中说明来源类别和出处。仅凭声明可以建立表面名称和签名，但不能建立未观察到的引擎行为。

## 验证

首先运行最相关的狭窄命令。标准项目检查记录在根目录 README 中。不要将不相关的失败作为证据或兼容性更改的一部分来“修复”。